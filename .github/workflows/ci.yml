name: CI - Lint, Test, Security & Ship Logs

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint-test-security:
    name: Lint, Test, Scan & Ship Logs
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: book_management
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -uroot -proot --silent" --health-interval=10s --health-timeout=5s --health-retries=5


    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v3

      - name: 🧠 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 💼 Install dependencies
        run: npm install

      - name: 🔧 Wait for MySQL
        run: |
          until mysqladmin ping -h 127.0.0.1 -uroot -proot --silent; do
            echo "Waiting for MySQL..." && sleep 3;
          done

      - name: 🚀 Run Migrations
        env:
          DB_USERNAME: root
          DB_PASSWORD: root
          DB_NAME: book_management
          DB_HOST: 127.0.0.1
        run: npx sequelize-cli db:migrate --env test

      - name: ✨ Run ESLint
        continue-on-error: true
        run: npm run lint | tee eslint-log.txt

      - name: 📊 Run Unit Tests
        env:
          DB_USERNAME: root
          DB_PASSWORD: root
          DB_NAME: book_management
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          NODE_ENV: test
        run: npm test | tee test-log.txt

      - name: 🔍 Run npm audit for vulnerability scan
        run: npm audit --audit-level=high || true | tee audit-log.txt

      - name: 🛡️ Run OWASP Dependency Check
        uses: dependency-check/Dependency-Check_Action@main
        with:
          project: "book-management-api"
          format: "HTML"
          out: "reports"

      - name: 📄 Upload OWASP Report
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: reports

      - name: 🚀 Ship logs to Logstash
        env:
          LOGSTASH_URL: ${{ secrets.LOGSTASH_URL }}
        run: |
          echo "Preparing payload..."
          ESLINT_LOG="$(cat eslint-log.txt || echo '')"
          TEST_LOG="$(cat test-log.txt || echo '')"
          AUDIT_LOG="$(cat audit-log.txt || echo '')"

          echo "Shipping logs to Logstash..."
          jq -n --arg lint "$ESLINT_LOG" --arg test "$TEST_LOG" --arg audit "$AUDIT_LOG" \
            '{lint_log: $lint, test_log: $test, audit_log: $audit, timestamp: now | todate}' > payload.json

          curl -X POST "$LOGSTASH_URL" -H "Content-Type: application/json" --data-binary @payload.json
